<launch>


  <node namespace="camera" name="gscam_driver_v4l" pkg="gscam" exec="gscam_node" output="screen">
    <param name="camera_name" value="default"/>
    <param name="camera_info_url" value="package://gscam/examples/uncalibrated_parameters.ini"/>
    <!--param name="gscam_config" value="nvarguscamerasrc sensor_id=0 !  video/x-raw(memory:NVMM),width=1920,height=1080,framerate=30/1 ! nvvidconv flip-method=2 ! video/x-raw, format=(string)BGRx ! videoconvert ! video/x-raw, format=(string)BGR"/-->
    <param name="gscam_config" value="nvarguscamerasrc sensor_id=0 ! nvvidconv flip-method=2 ! video/x-raw, format=(string)BGRx ! videoconvert ! video/x-raw, format=(string)BGR"/>
    <param name="sync_sink" value="true"/>
  </node>

</launch>