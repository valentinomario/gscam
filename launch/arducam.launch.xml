<launch>


  <node name="gscam_driver_v4l" pkg="gscam" exec="gscam_node" output="screen">
    <param name="camera_name" value="default"/>
    <param name="camera_info_url" value="package://gscam/examples/uncalibrated_parameters.ini"/>
    <!--param name="gscam_config" value="nvarguscamerasrc sensor_id=0 !  video/x-raw(memory:NVMM),width=1280,height=720,framerate=60/1 ! nvvidconv flip-method=0 ! video/x-raw, format=(string)BGRx ! videoconvert ! video/x-raw, format=(string)BGR"/-->
    <param name="gscam_config" value="nvarguscamerasrc sensor_id=0 ! video/x-raw(memory:NVMM),width=1920,height=1080,framerate=30/1 ! nvvidconv flip-method=0 ! video/x-raw, format=(string)GRAY8 ! videoscale ! video/x-raw, width=1024, height=576 ! videoconvert ! video/x-raw, format=(string)GRAY8"/>
    <param name="use_gst_timestamps" value="true"/>
    <param name="sync_sink" value="true"/>
  </node>

</launch>
